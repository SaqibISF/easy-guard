import React, { FC } from "react";
import HomeSection from "@/components/sections/HomeSection";

const HomePage: FC = () => (
  <>
    <HomeSection isHeroSection />
  </>
);

export default HomePage;
